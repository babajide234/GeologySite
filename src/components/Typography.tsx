// import { HeaderType } from "../types";

export const Typography = () => {
  return (
    <div>Typography</div>
  )
}



// export const Header = ({ level, children,...props }: HeaderType) => {
//   const Tag = `h${level || 1}`;

//   return <Tag {...props}>{children}</Tag>;
// };


